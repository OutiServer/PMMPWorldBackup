name: WorldBackup
description: World backup plugin for PocketMine-MP (4.x)
version: 1.0.0
api: 4.0.0
main: outiserver\worldbackup\WorldBackup
src-namespace-prefix: outiserver\worldbackup

permissions:
  worldbackup.command:
    description: WorldBackup Command Permission
    default: op